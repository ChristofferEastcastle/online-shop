spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;db_close_delay=-1
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672